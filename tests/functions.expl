{
    fact := function (k) {
    	total := 0
    	input total
    	t := total + (3+(total*3))
    	while k > 0 {
    		total := total + k
    		k := k - 1
    	}

    	return total+12
    }

    j := fact(10+5)
    _ := fact(10)
}